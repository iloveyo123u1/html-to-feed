<?xml version="1.0" encoding="UTF-8" ?>
<rss version="2.0">
    <channel>
        {% for item in feed %}
        <item>
            <title>{{ item.title }}</title>
            <description>{{ item.description }}</description>
        </item>
        {% endfor %}
    </channel>
</rss>
